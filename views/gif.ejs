<!DOCTYPE html>
<html>
	<head>
		<title>GIF Server</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/gif.css" />
		<script type="text/javascript">
			var nextImage = function() {
				setTimeout(
					function() {
						location.reload();
					},
					<%= gif.duration %>
				);
			}
		</script>
	</head>
	<body>
		<div id="gif">
		  	<img
		  		width="<%= gif.width %>"
		  		height="<%= gif.height %>"
		  		src="/<%= gif.filename %>"
		  		onload="nextImage()"
		  	/>
		<span id="description"><%= gif.title %></span>
		-
		<span id="author"><%= gif.author %></span>
		</div>
	</body>
</html>